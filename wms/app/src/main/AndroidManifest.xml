<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ykk.xc.com.wms">

    <!-- 权限录入 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:windowSoftInputMode="stateHidden|adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 仓库模块 -->
        <activity
            android:name=".warehouse.Cust_DialogActivity"
            android:theme="@style/dialogStyle" />
        <activity android:name=".warehouse.Ware_Pur_InActivity" />
        <activity android:name=".warehouse.Ware_Makter_CodeActivity" />
        <activity android:name=".LoadMoreActivity" />
        <activity android:name=".LoadMoreWrapperActivity" />
    </application>


</manifest>